# HTTP — redirect everything to HTTPS (except cert challenges)
server {
    listen 80;
    server_name YOURDOMAIN.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS — proxy only Streamlit
server {
    listen 443 ssl;
    server_name YOURDOMAIN.com;

    ssl_certificate /etc/letsencrypt/live/YOURDOMAIN.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/YOURDOMAIN.com/privkey.pem;

    # Security headers
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # Streamlit component assets
    location /component/ {
        proxy_pass http://spear:8501/component/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Streamlit static assets
    location /static/ {
        proxy_pass http://spear:8501/static/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    # Streamlit vendor assets
    location /vendor/ {
        proxy_pass http://spear:8501/vendor/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    # Streamlit websocket (required for live updates)
    location /_stcore/stream {
        proxy_pass http://spear:8501/_stcore/stream;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }

    # Streamlit health check
    location /_stcore/health {
        proxy_pass http://spear:8501/_stcore/health;
    }

    # Streamlit UI (catch-all — must be last)
    location / {
        proxy_pass http://spear:8501;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
        proxy_buffering off;
    }
}
